{
  "name": "@expressjs/perf-runner-vanilla",
  "version": "1.0.0",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "semistandard",
    "lint:fix": "semistandard --fix",
    "build": "node --run shared:build",
    "run": "node --run shared:run",
    "shared:build": "../runner-local-docker/scripts/build.sh 24 bookworm vanilla --force",
    "shared:run": "../runner-local-docker/scripts/run.sh"
  },
  "keywords": [
    "express",
    "performance",
    "benchmark",
    "vanilla",
    "node"
  ],
  "author": "",
  "license": "ISC",
  "description": "Vanilla Node.js runner for Express performance benchmarks",
  "dependencies": {
    "@expressjs/perf-runner-local-docker": "^1.0.0"
  },
  "devDependencies": {
    "semistandard": "^17.0.0"
  }
}
